[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "visit-create-v2-simulator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110,<1.0",
    "faker>=25.0,<40.0",
    "uvicorn[standard]>=0.29,<1.0",
]
