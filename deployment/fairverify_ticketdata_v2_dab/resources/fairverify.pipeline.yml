resources:
  pipelines:
    fairverify_pipeline:
      name: "LC-POC | FairVerify Ticketdata v2 | main"
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      development: true
      channel: PREVIEW
      libraries:
        - file:
            path: ../fairverify_ticketdata_v2_pipeline.py
        - file:
            path: ../_generated_fairverify_ticketdata_v2_python_source.py
      configuration:
        fairverify.base_url: ${var.cloudflare_url}/fairverify
        fairverify.event_id: ${var.event_id}
        fairverify.api_key: ${var.api_key}
        fairverify.access_token: ${var.access_token}
